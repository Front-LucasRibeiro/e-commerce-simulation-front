import Vue from 'vue'
import Vuetify from 'vuetify'

// Se você estiver usando Vuetify 3, a configuração pode ser diferente.
Vue.use(Vuetify)

export default new Vuetify({
  // Configurações do Vuetify
})
